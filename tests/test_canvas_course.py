import unittest
from httpx import AsyncClient
import asyncio


class TestCanvasCourse(unittest.TestCase):

    def setUp(self) -> None:
        # self.client = AsyncClient()
        pass

    # def test_client_type(self):
    #     self.assertEqual(type(self.client), AsyncClient)

